# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

GET		/connect					controllers.Application.connect
GET		/gcal					    controllers.Application.gcal
GET		/insert					    controllers.Application.insertEvent
GET		/sync					    controllers.Application.sync
GET		/removeAll					controllers.Application.removeAllEvents

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
